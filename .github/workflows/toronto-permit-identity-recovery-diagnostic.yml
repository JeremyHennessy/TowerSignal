name: Toronto Permit Identity Recovery Diagnostic

on:
  push:
    branches:
      - agent/toronto-building-permit-diagnostic-20260903
    paths:
      - .github/workflows/toronto-permit-identity-recovery-diagnostic.yml

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout diagnostic branch
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Compile recovery diagnostic
        run: python -m py_compile scripts/diagnose_toronto_permit_identity_recovery.py

      - name: Measure deterministic identity recoveries
        run: PYTHONPATH=scripts python scripts/diagnose_toronto_permit_identity_recovery.py --output toronto-permit-identity-recovery.json

      - name: Upload recovery report
        uses: actions/upload-artifact@v4
        with:
          name: toronto-permit-identity-recovery
          path: toronto-permit-identity-recovery.json
          retention-days: 7
