name: Patch round 2 history-mode test

on:
  push:
    branches: [agent/workflow-account-ux-round2-20260911]
    paths:
      - scripts/patch_round2_history_test.py
      - .github/workflows/patch-round2-history-test.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/workflow-account-ux-round2-20260911
      - name: Patch history-mode assertion
        run: python scripts/patch_round2_history_test.py
      - name: Commit test update
        run: |
          git config user.name "TowerSignal Agent"
          git config user.email "actions@users.noreply.github.com"
          git add tests/frontend/App.test.tsx
          git commit -m "Test account history mode"
          git push origin HEAD:agent/workflow-account-ux-round2-20260911
