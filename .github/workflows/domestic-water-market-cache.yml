name: Verify domestic-water market cache

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/domestic-water-market-cache.yml'
      - 'scripts/towersignal/domestic_water_market.py'
      - 'scripts/build_domestic_water_market_cache.py'
      - 'scripts/validate_domestic_water_market_cache.py'
      - 'scripts/towersignal/provider_resolution.py'
      - 'tests/python/test_provider_resolution.py'
      - 'docs/BUILD017_DOMESTIC_WATER_PROVIDER_INTELLIGENCE.md'

permissions:
  contents: read

concurrency:
  group: towersignal-domestic-water-market-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build-cache:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build exact-count domestic-water market cache
        run: python scripts/build_domestic_water_market_cache.py --output domestic-water-market/cache.json --page-size 50000
      - name: Validate provider, lab and DEC evidence boundaries
        run: python scripts/validate_domestic_water_market_cache.py --cache domestic-water-market/cache.json --max-age-days 1 --require-production-volume
      - name: Run provider-resolution unit tests
        run: python -m unittest tests.python.test_provider_resolution -v
      - name: Upload verified domestic-water market cache
        uses: actions/upload-artifact@v4
        with:
          name: towersignal-domestic-water-market-cache
          path: domestic-water-market/cache.json
          retention-days: 7

  integration:
    if: ${{ github.event_name == 'pull_request' }}
    needs: build-cache
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Download verified domestic-water market cache
        uses: actions/download-artifact@v4
        with:
          name: towersignal-domestic-water-market-cache
          path: .verified-domestic-water-market
      - name: Revalidate downloaded cache
        run: python scripts/validate_domestic_water_market_cache.py --cache .verified-domestic-water-market/cache.json --max-age-days 1 --require-production-volume
      - name: Install frontend dependencies
        run: npm install --no-audit --no-fund
      - name: Python tests
        run: python -m unittest discover -s tests/python -p 'test_*.py' -v
      - name: Lint frontend
        run: npm run lint
      - name: Typecheck frontend
        run: npm run typecheck
      - name: Frontend tests
        run: npm test
      - name: Production build
        run: npm run build
